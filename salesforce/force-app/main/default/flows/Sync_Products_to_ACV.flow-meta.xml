<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>59.0</apiVersion>
    <description>DEPRECATED - This flow is intentionally disabled. ACV should never be auto-updated from products. ACV is the source of truth.</description>
    <interviewLabel>Sync Products to ACV {!$Flow.CurrentDateTime}</interviewLabel>
    <label>Sync Products to ACV</label>
    <processMetadataValues>
        <name>BuilderType</name>
        <value>
            <stringValue>LightningFlowBuilder</stringValue>
        </value>
    </processMetadataValues>
    <processType>AutoLaunchedFlow</processType>
    
    <!-- This flow intentionally does nothing - just ends immediately -->
    <start>
        <locationX>50</locationX>
        <locationY>0</locationY>
        <object>OpportunityLineItem</object>
        <recordTriggerType>CreateAndUpdate</recordTriggerType>
        <triggerType>RecordAfterSave</triggerType>
    </start>
    
    <status>Active</status>
</Flow>
